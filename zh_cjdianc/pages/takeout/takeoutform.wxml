<view class="gif" wx:if="{{isloading}}">
    <image src="../../img/jzdh(1).gif"></image>
</view>
<!---------------------------------------------------- 提交订单1 外卖、自提......--------------------------------->

<view wx:if="{{!isloading}}">
<view wx:if="{{index == 0 || index == 1}}">
<block>
    <import src="/zh_cjdianc/pages/commons/xy.wxml"></import>
    <view class="page">
        <!-- 顶部背景图片 -->
        <view class="head-bg" style="background:{{color}}"></view>

        <view class="formmain">
            <view class="formhead flex-bet flex-y-center" style="background:{{color}}"> </view> 
            <!-- 外卖配送 -->
            <view class="formhdwm" wx:if="{{index == 0}}">
                <view class="weui-cells weui-cells_after-title">
                    <!-- 收货地址 有默认收货地址-->
                    <navigator class="weui-cell weui-cell_access wmaddress" hoverClass="weui-cell_active" style="border-bottom: 1rpx solid #f4f4f4;" url="../wddz/xzdz" wx:if="{{address}}">
                        <view class="weui-cell__bd">
                            <view class="wmshr">
                                <text>{{address.user_name}}</text>
                                <text wx:if="{{address.sex=='1'}}">先生</text>
                                <text wx:if="{{address.sex=='2'}}">女士</text>
                                <text>{{address.tel}}</text>
                            </view>
                            <view class="wmshdz">{{address.area}}{{address.address}}</view>
                        </view>
                        <view class="weui-cell__ft weui-cell__ft_in-access"></view>
                    </navigator>

                    <!-- 收货地址 没有默认收货地址 -->
                    <navigator class="weui-cell weui-cell_access wmaddress" hoverClass="weui-cell_active" style="border-bottom: 1rpx solid #f4f4f4;" url="../wddz/xzdz" wx:else>
                        <view class="weui-cell__bd">请选择收货地址</view>
                        <view class="weui-cell__ft weui-cell__ft_in-access"></view>
                    </navigator>

                <!-- 订单类型 -->
                <picker bindchange="bindChange" value="{{index}}" range="{{array}}"  wx:if="{{jiacai != 1 &&jiacai != 2}}">
                        <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                                <image src='../../img/ddlx.png' style="margin-right: 5px;vertical-align: middle;width:45rpx; height:45rpx;"></image>             
                            <view class="weui-cell__bd" style='font-size:35rpx;'>订单类型</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" style="font-size:35rpx;color:#000;">{{array[index]}}</view>
                        </view>
                 </picker>

<!-- 加菜时显示--------------- -->
                <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active" wx:if="{{jiacai == 1 || jiacai == 2}}">
                    <image src='../../img/ddlx.png' style="margin-right: 5px;vertical-align: middle;width:45rpx; height:45rpx;"></image>
                    <view class="weui-cell__bd" style='font-size:35rpx;'>订单类型</view>
                    <view class="weui-cell__ft" style="font-size:35rpx;color:#000;" wx:if="{{jiacai == 1}}">外卖（加菜）</view>
                </view>


                    <picker style=' border-top: 1rpx solid #efeff4;'  bindchange="bindPickerChange" range="{{wmtimearray}}" value="{{wmindex}}">
                        <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                                <image src="../../img/wmtime.png" style="margin-right: 5px;vertical-align: middle;width:35rpx; height:35rpx;"></image>
                            <view class="weui-cell__bd">立即送出</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" style="color:{{color}}">大约{{wmtimearray[wmindex]}}送达</view>
                        </view>
                    </picker>
                </view>
            </view>

            <!-- 到店自提 -->
            <view class="formhdzq" wx:if="{{index == 1}}">
                <view class="weui-cells weui-cells_after-title">
                    <view bindtap="ckwz" class="weui-cell weui-cell_access zqaddress" data-jwd="{{store.coordinates}}" hoverClass="weui-cell_active" url="">
                        <view class="weui-cell__bd">
                            <view class="zqdz1">自取地址</view>
                            <view class="zqdz2">{{store.address}}</view>
                            <view class="zqdz3">
                                <text>查看地图</text>
                            </view>
                        </view>
                        <view class="weui-cell__ft weui-cell__ft_in-access"></view>
                    </view> 


                 <picker style=' border-top: 1rpx solid #efeff4;' bindchange="bindChange" value="{{index}}" range="{{array}}"  wx:if="{{jiacai != 1 &&jiacai != 2}}">
                        <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                                <image src='../../img/ddlx.png' style="margin-right: 5px;vertical-align: middle;width:45rpx; height:45rpx;"></image>
                            <view class="weui-cell__bd" style='font-size:35rpx;'>订单类型</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" style="font-size:35rpx;color:#000;">{{array[index]}}</view>
                        </view>
                 </picker>

                 <!-- 加菜时显示 -->
                 <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active" wx:if="{{jiacai == 1 || jiacai == 2}}">
                                <image src='../../img/ddlx.png' style="margin-right: 5px;vertical-align: middle;width:45rpx; height:45rpx;"></image>
                            <view class="weui-cell__bd" style='font-size:35rpx;'>订单类型</view>
                             <view class="weui-cell__ft" style="font-size:35rpx;color:#000;" wx:if="{{jiacai == 2}}">自提（加菜）</view>
                 </view>

                    <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active" url="">
                        <view class="weui-cell__bd">自取时间</view>
                        <view class="weui-cell__ft weui-cell__ft_in-access picker" style="color:{{color}}">
                            <picker bindchange="bindDateChange" mode="date" start="{{datestart}}" style=" width: 200rpx" value='{{data}}'>
                                <view class="weui-input flex-row flex-y-center">{{date}}</view>
                            </picker>
                            <picker bindchange="bindTimeChange" end="23:59" mode="time" start="{{timestart}}" style=" width: 120rpx" value="{{time}}">
                                <view class="weui-input flex-row flex-y-center flex-x-center">{{time}}</view>
                            </picker>
                        </view>
                    </view>

                    <view class="weui-cell weui-cell_access" style="padding:8rpx 30rpx;">
                        <view class="weui-cell__bd">自取电话</view>
                        <view class="weui-cell__ft">
                            <input bindinput="KeyMobile" class="weui-input" placeholder="请输入自取联系电话" type="number" value="{{mobile}}"></input>
                        </view>
                    </view>

                    <view class="weui-cell weui-cell_access" style="padding:8rpx 30rpx;">
                        <view class="weui-cell__bd">自取联系人</view>
                        <view class="weui-cell__ft">
                            <input bindinput="KeyName" class="weui-input" placeholder="请输入自取联系人" value="{{name}}"></input>
                        </view>
                    </view>

                    <label bindtap="checkboxChange" class="weui-cell weui-check__label">
                        <view class="weui-cell__hd weui-check__hd_in-checkbox">
                            <icon class="weui-icon-checkbox_circle" size="21" type="circle" wx:if="{{!checked}}"></icon>
                            <icon class="weui-icon-checkbox_success" color="#34aaff" size="21" type="success" wx:if="{{checked}}"></icon>
                        </view>
                        <view catchtap="openxy" class="weui-cell__bd">同意
                           <text bindtap="look" style="color:{{color}}">《到店自取服务协议》</text>
                        </view>
                    </label>
                </view>
            </view>

<!-- --外卖自提-----订单的信息----------------------------------------------------------------------------------------- -->
            <view class="formbd">

                <view class="flex-row sjname" style=' border-top: 1rpx solid #efeff4;'>
                    <image src="{{url}}{{store.logo}}"></image>
                    <view class="flex-grow-1">{{store.name}}</view>
                </view>

                <view class="cart-item flex-row flex-y-center" wx:for="{{cart_list}}" wx:key="*this">
                    <view class="flex-grow-0 flex-y-center">
                        <image class="goods-pic" mode="aspectFill" src="{{item.img}}"></image>
                    </view>
                    
                    <view class="flex-grow-1 flex-col">
                        <view class="flex-grow-1">
                            <view wx:if="{{item.oneTwo===1}}">原</view>
                            <view wx:if="{{item.oneTwo===2}}">新</view>
                            <view class="goods-name">{{item.name}}</view>
                            <view class="attr-list">
                                <view class="attr-item">{{item.spec}}</view>
                            </view>
                        </view>

                        <view class="flex-grow-0 flex-row">
                            <view class="flex-grow-1 num">×{{item.num}}</view>
                            <view class="flex-grow-0 price">单价:￥{{item.price}}</view>
                            <view wx:if="{{}}" style='text-decoration:line-through' class="flex-grow-0 price">单价:￥{{item.price}}</view>
                        </view>
                    </view>
                </view>


                <view class="formbdyh flex-row flex-y-center" wx:if="{{pack_money!='' || pack_money!= 'undefined'}}">
                    <view class="flex-row flex-y-center flex-grow-1">
                         <image src='../../img/boxmoney.png' style="width:50rpx;height:50rpx;"></image>

                        <view>包装费</view>

                    </view>
                    <view class="flex-grow-0">￥{{pack_money}}</view>
                </view>



                <view style=' border-top: 1rpx solid #efeff4;' class="formbdyh flex-row flex-y-center" wx:if="{{index == 0}}">
                    <view class="flex-row flex-y-center flex-grow-1">
                         <image src='../../img/Delivery.png' style="width:65rpx;height:60rpx;"></image>
                        <view>配送费</view>
                    </view>

                    <view class="flex-grow-0" wx:if="{{index == 0}}">￥{{psf}}</view>
                    <view class="flex-grow-0" style="color:#999;" wx:if="{{index == 1}}">到店自取无配送费</view>
                </view>


                <!-- 获得积分 -->
                <view style='border-top: 1rpx solid #efeff4;' class="formbdyh flex-row flex-y-center" wx:if="{{getIntegral>0 && pay_type != '4'}}">
                    <view class="flex-row flex-y-center flex-grow-1">
                        <view class="yhhead bao">积</view>
                        <view>获得积分</view>
                    </view>
                    <view class="flex-grow-0">{{getIntegral}}分</view>
                
                </view>

                <!-- 在线支付满减 -->
                <view class="formftyh">
                <!-- 折扣 -->
                 <view class="formbdyh flex-row flex-y-center" wx:if='{{mjmoney!=0}}'>

                        <view class="flex-row flex-y-center flex-grow-1">
                             <image src='../../img/Discount.png' style="width:50rpx;height:30rpx;"></image>
                            <view>会员折扣</view>
                        </view>                        
                        <view class="flex-grow-0">-￥{{mjmoney}}</view>                                          
                        
                </view>
   
                   <!-- 在线支付满减 -->                  
                     <view class="formbdyh flex-row flex-y-center" wx:if="{{mj_reduce > 0 && pay_type != '4'}}">
                        <view class="flex-row flex-y-center flex-grow-1">
                           <image src='../../img/reduction.png' style="width:50rpx;height:30rpx;"></image>
                            <view>在线支付满减</view>
                        </view>                       

                         <view class="flex-grow-0">-￥{{mj_reduce}}</view>

                        <view class="flex-grow-0" style="color:#999;" wx:if="{{isnewuser=='1'}}">新用户不享有此优惠</view>                            </view>

                    <!-- 选择积分、支付部分积分 -->                  
                     <view class="formbdyh flex-row flex-y-center" wx:if='{{totalPrice>90&&used_total_score>=100 && pay_type != "4"}}'>
                        <radio color="{{color}}" style="transform:scale(0.8);" wx:for-items="{{items}}" checked="{{item.checked}}" data-index="index" bindtap="click"></radio>
                        <view class="flex-row flex-y-center flex-grow-1">
                            <view class="weui-cell__bd">使用{{used_total_score}}积分</view>
                        </view>
                        <view wx:if="{{!ischeck == true}}">请选择</view>
                        <view wx:elif="{{(ischeck==true)&&(totalPrice>90&&used_total_score>=100&&used_total_score<200)}}">-￥10</view>
                        <view wx:elif="{{(ischeck==true)&&(totalPrice>190&&used_total_score>=200&&used_total_score<300)}}">-￥20</view>
                        <view wx:else="{{(ischeck==true)&&(totalPrice>290&&used_total_score>=300)}}">-￥30</view>
                     </view>

                    <!-- 新客户下单立减 -->                   
                    <view class="formbdyh flex-row flex-y-center" wx:if="{{storeset.xyh_open=='1'&&isnewuser=='1'}}">
    
                        <view class="flex-row flex-y-center flex-grow-1">
                            <image src='../../img/new.png' style="width:50rpx;height:50rpx;"></image>
                            <view>新客户下单立减</view>
                        </view>
                        <view class="flex-grow-0">-￥{{storeset.xyh_money}}</view>
                    </view>

                   <view class="weui-cells weui-cells_after-title" style="border-top: 1rpx solid #f4f4f4;"  wx:if="{{yhq_active && pay_type != '4'}}">

                        <navigator bindtap="showcart" class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                            <view class="weui-cell__hd">
                                <image mode="aspectFit" src="../../img/personal/youhuiquan.png" style="margin-right: 15rpx;vertical-align: middle;width:40rpx; height: 40rpx;"></image>
                            </view>
                            <view class="weui-cell__bd">商家优惠券</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" wx:if="{{Coupons.length==0}}">暂无此商家优惠券</view>
<view class="weui-cell__ft_in-access" wx:if="{{Coupons.length>0&&yhqcoupon_reduce==0}}">
                            选择优惠券</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{Coupons.length>0&&yhqcoupon_reduce!=0}}">-￥{{yhqcoupon_reduce}}</view>

                        </navigator>
                    </view>

                   <view class="weui-cells weui-cells_after-title" style="border-top: 1rpx solid #f4f4f4;"  wx:if="{{hb_active && pay_type != '4'}}">

                        <navigator bindtap="hbshowcart" class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                            <view class="weui-cell__hd">
                                <image mode="aspectFit" src="../../img/hb.png" style="margin-right: 15rpx;vertical-align: middle;width:40rpx; height: 40rpx;"></image>
                            </view>
                            <view class="weui-cell__bd">红包</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" wx:if="{{hbarr.length==0}}">暂无红包</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{hbarr.length>0&&hbcoupon_reduce==0}}">选择红包</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{hbarr.length>0&&hbcoupon_reduce!=0}}">-￥{{hbcoupon_reduce}}</view>
                        </navigator>
                    </view>
                    <view class="sfk flex-row flex-y-center">
                        <view class="sfk-fv flex-grow-0">实付款</view>
                        <view class="flex-grow-1 xiaoji">
                            <view></view>
                            <view>小计</view>

                            <view wx:if="{{exchange_goods}}">￥{{totalPrice}}</view>
                            <view wx:if="{{!exchange_goods}}">{{integral_money}}积分</view>
                            <!-- <view wx:if="{{!exchange_goods}}" style='text-decoration:line-through;'>￥{{totalPrice}}</view> -->
                        </view>
                    </view>
                </view>

                <view class="formft">
                    <view class="weui-cells weui-cells_after-title">
                        <picker bindchange="bindcjPickerChange" range="{{cjarray}}" value="{{cjindex}}" wx:if="{{storeset.is_cj=='1'}}">
                            <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                                <view class="weui-cell__bd">{{storeset.cj_name}}</view>
                                <view class="weui-cell__ft weui-cell__ft_in-access">{{cjarray[cjindex]}}</view>
                            </view>
                        </picker>
                        <navigator class="weui-cell weui-cell_access" hoverClass="weui-cell_active" style="border-bottom: 1rpx solid #efeff4;" url="note">
                            <view class="weui-cell__bd" style="min-width:120rpx;">备注</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access">{{note}}</view>
                        </navigator>
                    </view>
                </view>
            </view>
        </view>
    </view>


    <view class="footmj" wx:if="{{reduction.length>0&&mjindex!=null&&isnewuser=='2'}}">
      下单已满{{reduction[mjindex].full}}元，可减
      <text>{{reduction[mjindex].reduction}}</text>元
    </view>

    <view class="dbtjdd flex-row flex-y-center">
        <view class="dbtjddl flex-row flex-y-center">
            <view class="flex-grow-1">合计
              <text wx:if="{{exchange_goods}}">￥{{totalPrice}}</text>
              <text wx:if="{{!exchange_goods}}">{{integral_money}}积分</text>
              <!-- <text wx:if="{{!exchange_goods}}" style='text-decoration:line-through;'>￥{{totalPrice}}</text> -->
            </view>
            
        </view>
        <view class="dbtjddr">
            <form bindreset="formReset" bindsubmit="tjddformSubmit" reportSubmit="true">
                <button formType="submit" style="background:{{color}}">提交订单</button>
            </form>
        </view>
    </view>

</block>

<!-- 优惠券 -->
<view catchtap="closecart" class="share-modal {{share_modal_active?'active':''}}">
    <view class="share-modal-body">
        <view class="yhqhead">选择优惠券</view>
        <radio-group bindchange="radioChange" class="radio-group" name="radiogroup">
            <scroll-view scrollY style="height: 400rpx;">
                <label catchtap="xzq" data-full="{{item.full}}" data-kdje="{{item.reduce}}" data-rdid="{{item.id}}" data-type="代金券" wx:for="{{Coupons}}">
                    <view class="mdlist">
                        <view class="coupon-item unreceive" id="{{item.coupon_id}}">
                            <view class="item-left" style="background:#F65C5C">
                                <text class="num">{{item.reduce}}</text>
                                <text class="unit">元</text>
                            </view>
                            <view class="item-right">
                                <text class="name">{{item.name}}</text>
                                <text class="conditions" wx:if="{{item.full!='0.0'}}">满{{item.full}}可用</text>
                                <text class="conditions" wx:else>无门槛</text>
                                <text class="prefer">有效期至{{item.end_time}}</text>
                            </view>
                        </view>
                        <view class="radio">
                            <radio checked="{{activeradio==item.id}}" color="{{color}}" disabled="{{gwcinfo.money<item.full}}" value="{{item.id}}"></radio>
                        </view>
                    </view>
                </label>
                <view class="kong" wx:if="{{Coupons.length==0}}">
                    <image src="../../img/wushuju.png"></image>
                    <view>暂无此门店的优惠券</view>
                </view>
            </scroll-view>
        </radio-group>
    </view>
</view>

<!-- 红包 -->
<view catchtap="hbclosecart" class="share-modal {{hbshare_modal_active?'active':''}}">
    <view class="share-modal-body">
        <view class="yhqhead">选择红包</view>
        <radio-group bindchange="hbradioChange" class="radio-group" name="hbradiogroup">
            <scroll-view scrollY style="height: 400rpx;">
                <label catchtap="xzhb" data-full="{{item.full}}" data-kdje="{{item.reduce}}" data-rdid="{{item.id}}" data-type="红包" wx:for="{{hbarr}}">
                    <view class="mdlist">
                        <view class="coupon-item unreceive" id="{{item.coupon_id}}">
                            <view class="item-left" style="background:#F65C5C">
                                <text class="num">{{item.reduce}}</text>
                                <text class="unit">元</text>
                            </view>
                            <view class="item-right">
                                <text class="name">{{item.name}}</text>
                                <text class="conditions" wx:if="{{item.full!='0.0'}}">满{{item.full}}可用</text>
                                <text class="conditions" wx:else>无门槛</text>
                                <text class="prefer">有效期至{{item.end_time}}</text>
                            </view>
                        </view>
                        <view class="radio">
                            <radio checked="{{hbactiveradio==item.id}}" color="{{color}}" disabled="{{gwcinfo.money<item.full}}" value="{{item.id}}"></radio>
                        </view>
                    </view>
                </label>
                <view class="kong" wx:if="{{hbarr.length==0}}">
                    <image src="../../img/wushuju.png"></image>
                    <view>暂无可用红包</view>
                </view>
            </scroll-view>
        </radio-group>
    </view>
</view>
</view>


<!-------------------------------- 提交订单2 到店扫码点餐......------------------------------------------------------------------------->
<view wx:if="{{index == 3}}">
<block>
    <import src="/zh_cjdianc/pages/commons/xy.wxml"></import>

    <view class="page">
        <!-- 顶部背景图片 -->
        <view class="table_num" style="background:{{color}}">
          <text>桌号 ：{{table_num}}</text>
        </view>

        <view class="formmain"> 
<!-- -----到店扫码---订单的信息----------------------------------------------------------------------------------------- -->
            <view class="formbd">
                <view class="flex-row sjname">
                    <image src="{{url}}{{store.logo}}"></image>
                    <view class="flex-grow-1">{{store.name}}</view>
                </view>

                <view class="cart-item flex-row flex-y-center" wx:for="{{cart_list}}" wx:key="*this">
                    <view class="flex-grow-0 flex-y-center">
                        <image class="goods-pic" mode="aspectFill" src="{{item.img}}"></image>
                    </view>
                    
                    <view class="flex-grow-1 flex-col">
                        <view class="flex-grow-1">   
                            <view wx:if="{{item.oneTwo===1}}">原</view>
                            <view wx:if="{{item.oneTwo===2}}">新</view>
                            <view class="goods-name">{{item.name}}</view>
                            <view class="attr-list">
                                <view class="attr-item">{{item.spec}}</view>
                            </view>
                        </view>

                        <view class="flex-grow-0 flex-row">
                            <view class="flex-grow-1 num">×{{item.num}}</view>
                            <view class="flex-grow-0 price">单价:￥{{item.price}}</view>
                        </view>
                    </view>
                </view>

                <!-- 积分，完善 -->
                <view class="formbdyh flex-row flex-y-center" wx:if="{{getIntegral>0}}">
                    <view class="flex-row flex-y-center flex-grow-1">
                        <view class="yhhead bao">积</view>
                        <view>获得积分</view>
                    </view>
                    <view class="flex-grow-0">{{getIntegral}}分</view>
                </view>
     
                <!-- 在线支付满减 -->
                <view class="formftyh">
                <!-- 折扣 -->
                  <view class="formbdyh flex-row flex-y-center" wx:if='{{mjmoney!=0}}'>
                        <view class="flex-row flex-y-center flex-grow-1">
                            <image src='../../img/Discount.png' style="width:50rpx;height:30rpx;"></image>
                            <view>会员折扣</view>
                        </view>                        
                        <view class="flex-grow-0">-￥{{mjmoney}}</view>                                                 </view>
   
                   <!-- 在线支付满减 -->                  

                     <view class="formbdyh flex-row flex-y-center"  wx:if='{{mj_reduce > 0}}'>
                        <view class="flex-row flex-y-center flex-grow-1">
                             <image src='../../img/reduction.png' style="width:50rpx;height:30rpx;"></image>
                            <view>在线支付满减</view>
                        </view>                       

                        <view class="flex-grow-0">-￥{{mj_reduce}}</view>

                        <view class="flex-grow-0" style="color:#999;" wx:if="{{isnewuser=='1'}}">新用户不享有此优惠</view>                            </view>

                     <!-- 选择积分、支付部分积分 -->                  
                     <view class="formbdyh flex-row flex-y-center" wx:if='{{totalPrice>90&&used_total_score>=100}}'>
                     <radio color="{{color}}" style="transform:scale(0.8);" wx:for-items="{{items}}" checked="{{item.checked}}" data-index="index" bindtap="click"></radio>
                        <view class="flex-row flex-y-center flex-grow-1">
                            <view class="weui-cell__bd">使用{{used_total_score}}积分</view>
                        </view>
                        <view wx:if="{{!ischeck == true}}">请选择</view>
                        <view wx:elif="{{(ischeck==true)&&(totalPrice>90&&used_total_score>=100&&used_total_score<200)}}">-￥10</view>
                        <view wx:elif="{{(ischeck==true)&&(totalPrice>190&&used_total_score>=200&&used_total_score<300)}}">-￥20</view>
                        <view wx:else="{{(ischeck==true)&&(totalPrice>290&&used_total_score>=300)}}">-￥30</view>
                     </view>

                    <!-- 新客户下单立减 -->                   
                    <view class="formbdyh flex-row flex-y-center" wx:if="{{storeset.xyh_open=='1'&&isnewuser=='1'}}">
                        <view class="flex-row flex-y-center flex-grow-1">
                          <image src='../../img/new.png' style="width:50rpx;height:50rpx;"></image>
                            <view>新客户下单立减</view>
                        </view>
                        <view class="flex-grow-0">-￥{{storeset.xyh_money}}</view>
                    </view>

                    <view class="weui-cells weui-cells_after-title" style="border-top: 1rpx solid #f4f4f4;"  wx:if='{{yhq_active}}'>
                        <navigator bindtap="showcart" class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                            <view class="weui-cell__hd">
                                <image mode="aspectFit" src="../../img/personal/youhuiquan.png" style="margin-right: 15rpx;vertical-align: middle;width:40rpx; height: 40rpx;"></image>
                            </view>
                            <view class="weui-cell__bd">商家优惠券</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" wx:if="{{Coupons.length==0}}">暂无此商家优惠券</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{Coupons.length>0&&yhqcoupon_reduce==0}}">选择优惠券</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{Coupons.length>0&&yhqcoupon_reduce!=0}}">-￥{{yhqcoupon_reduce}}</view>
                        </navigator>
                    </view>

                    <view class="weui-cells weui-cells_after-title" style="border-top: 1rpx solid #f4f4f4;"  wx:if='{{hb_active}}'>
                        <navigator bindtap="hbshowcart" class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                            <view class="weui-cell__hd">
                                <image mode="aspectFit" src="../../img/hb.png" style="margin-right: 15rpx;vertical-align: middle;width:40rpx; height: 40rpx;"></image>
                            </view>
                            <view class="weui-cell__bd">红包</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" wx:if="{{hbarr.length==0}}">暂无红包</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{hbarr.length>0&&hbcoupon_reduce==0}}">选择红包</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{hbarr.length>0&&hbcoupon_reduce!=0}}">-￥{{hbcoupon_reduce}}</view>
                        </navigator>
                    </view>
                    <view class="sfk flex-row flex-y-center">
                        <view class="sfk-fv flex-grow-0">实付款</view>
                        <view class="flex-grow-1 xiaoji">
                            <view></view>
                            <view>小计</view>

                            <view wx:if="{{exchange_goods}}">￥{{totalPrice}}</view>
                            <view wx:if="{{!exchange_goods}}">{{integral_money}}积分</view>
                            <!-- <view wx:if="{{!exchange_goods}}" style='text-decoration:line-through;'>￥{{totalPrice}}</view> -->
                        </view>
                    </view>
                </view>

                <view class="formft">
                    <view class="weui-cells weui-cells_after-title">
                        <picker bindchange="bindcjPickerChange" range="{{cjarray}}" value="{{cjindex}}" wx:if="{{storeset.is_cj=='1'}}">
                            <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                                <view class="weui-cell__bd">{{storeset.cj_name}}</view>
                                <view class="weui-cell__ft weui-cell__ft_in-access">{{cjarray[cjindex]}}</view>
                            </view>
                        </picker>


                        <navigator class="weui-cell weui-cell_access" hoverClass="weui-cell_active" style="border-bottom: 1rpx solid #efeff4;" url="note">
                            <view class="weui-cell__bd">备注</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access">{{note}}</view>
                        </navigator>
                    </view>
                </view>
            </view>
        </view>
    </view>


    <view class="footmj" wx:if="{{reduction.length>0&&mjindex!=null&&isnewuser=='2'}}">
      下单已满{{reduction[mjindex].full}}元，可减
      <text>{{reduction[mjindex].reduction}}</text>元
    </view>

    <view class="dbtjdd flex-row flex-y-center">
        <view class="dbtjddl flex-row flex-y-center">
            <view class="flex-grow-1">合计
              <text wx:if="{{exchange_goods}}">￥{{totalPrice}}</text>
              <text wx:if="{{!exchange_goods}}">{{integral_money}}积分</text>
              <!-- <text wx:if="{{!exchange_goods}}" style='text-decoration:line-through;'>￥{{totalPrice}}</text> -->
            </view>
            
        </view>
        <view class="dbtjddr">
            <form bindreset="formReset" bindsubmit="tjddformSubmit" reportSubmit="true">
                <button formType="submit" style="background:{{color}}">提交订单</button>
            </form>
        </view>
    </view>

</block>

<!-- 优惠券 -->
<view catchtap="closecart" class="share-modal {{share_modal_active?'active':''}}">
    <view class="share-modal-body">
        <view class="yhqhead">选择优惠券</view>
        <radio-group bindchange="radioChange" class="radio-group" name="radiogroup">
            <scroll-view scrollY style="height: 400rpx;">
                <label catchtap="xzq" data-full="{{item.full}}" data-kdje="{{item.reduce}}" data-rdid="{{item.id}}" data-type="代金券" wx:for="{{Coupons}}">
                    <view class="mdlist">
                        <view class="coupon-item unreceive" id="{{item.coupon_id}}">
                            <view class="item-left" style="background:#F65C5C">
                                <text class="num">{{item.reduce}}</text>
                                <text class="unit">元</text>
                            </view>
                            <view class="item-right">
                                <text class="name">{{item.name}}</text>
                                <text class="conditions" wx:if="{{item.full!='0.0'}}">满{{item.full}}可用</text>
                                <text class="conditions" wx:else>无门槛</text>
                                <text class="prefer">有效期至{{item.end_time}}</text>
                            </view>
                        </view>
                        <view class="radio">
                            <radio checked="{{activeradio==item.id}}" color="{{color}}" disabled="{{gwcinfo.money<item.full}}" value="{{item.id}}"></radio>
                        </view>
                    </view>
                </label>
                <view class="kong" wx:if="{{Coupons.length==0}}">
                    <image src="../../img/wushuju.png"></image>
                    <view>暂无此门店的优惠券</view>
                </view>
            </scroll-view>
        </radio-group>
    </view>
</view>

<!-- 红包 -->
<view catchtap="hbclosecart" class="share-modal {{hbshare_modal_active?'active':''}}">
    <view class="share-modal-body">
        <view class="yhqhead">选择红包</view>
        <radio-group bindchange="hbradioChange" class="radio-group" name="hbradiogroup">
            <scroll-view scrollY style="height: 400rpx;">
                <label catchtap="xzhb" data-full="{{item.full}}" data-kdje="{{item.reduce}}" data-rdid="{{item.id}}" data-type="红包" wx:for="{{hbarr}}">
                    <view class="mdlist">
                        <view class="coupon-item unreceive" id="{{item.coupon_id}}">
                            <view class="item-left" style="background:#F65C5C">
                                <text class="num">{{item.reduce}}</text>
                                <text class="unit">元</text>
                            </view>
                            <view class="item-right">
                                <text class="name">{{item.name}}</text>
                                <text class="conditions" wx:if="{{item.full!='0.0'}}">满{{item.full}}可用</text>
                                <text class="conditions" wx:else>无门槛</text>
                                <text class="prefer">有效期至{{item.end_time}}</text>
                            </view>
                        </view>
                        <view class="radio">
                            <radio checked="{{hbactiveradio==item.id}}" color="{{color}}" disabled="{{gwcinfo.money<item.full}}" value="{{item.id}}"></radio>
                        </view>
                    </view>
                </label>
                <view class="kong" wx:if="{{hbarr.length==0}}">
                    <image src="../../img/wushuju.png"></image>
                    <view>暂无可用红包</view>
                </view>
            </scroll-view>
        </radio-group>
    </view>
</view>
</view>

<!------------------------------------------------ 预约订单-------------------------------------- -->
<view wx:if="{{index == 4}}">
<block>
    <import src="/zh_cjdianc/pages/commons/xy.wxml"></import>
    <view class="page">
        <!-- 顶部背景图片 -->
        <view class="head-bg" style="background:{{color}}"></view>

        <view class="formmain">
            <view class="formhead flex-bet flex-y-center" style="background:{{color}}"> </view> 
            <!-- 外卖配送 -->
            <view class="formhdwm" wx:if="{{index == 0}}">
                <view class="weui-cells weui-cells_after-title">
                    <!-- 收货地址 有默认收货地址-->
                    <navigator class="weui-cell weui-cell_access wmaddress" hoverClass="weui-cell_active" style="border-bottom: 1rpx solid #f4f4f4;" url="../wddz/xzdz" wx:if="{{address}}">
                        <view class="weui-cell__bd">
                            <view class="wmshr">
                                <text>{{address.user_name}}</text>
                                <text wx:if="{{address.sex=='1'}}">先生</text>
                                <text wx:if="{{address.sex=='2'}}">女士</text>
                                <text>{{address.tel}}</text>
                            </view>
                            <view class="wmshdz">{{address.area}}{{address.address}}</view>
                        </view>
                        <view class="weui-cell__ft weui-cell__ft_in-access"></view>
                    </navigator>

                    <!-- 收货地址 没有默认收货地址 -->
                    <navigator class="weui-cell weui-cell_access wmaddress" hoverClass="weui-cell_active" style="border-bottom: 1rpx solid #f4f4f4;" url="../wddz/xzdz" wx:else>
                        <view class="weui-cell__bd">请选择收货地址</view>
                        <view class="weui-cell__ft weui-cell__ft_in-access"></view>
                    </navigator>

                <!-- 订单类型 -->
                <picker bindchange="bindChange" value="{{index}}" range="{{array}}"  wx:if="{{jiacai != 1 &&jiacai != 2}}">
                        <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                                <image src='../../img/ddlx.png' style="margin-right: 5px;vertical-align: middle;width:45rpx; height:45rpx;"></image>             
                            <view class="weui-cell__bd" style='font-size:35rpx;'>订单类型</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" style="font-size:35rpx;color:#000;">{{array[index]}}</view>
                        </view>
                 </picker>

<!-- 加菜时显示--------------- -->
                <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active" wx:if="{{jiacai == 1 || jiacai == 2}}">
                    <image src='../../img/ddlx.png' style="margin-right: 5px;vertical-align: middle;width:45rpx; height:45rpx;"></image>
                    <view class="weui-cell__bd" style='font-size:35rpx;'>订单类型</view>
                    <view class="weui-cell__ft" style="font-size:35rpx;color:#000;" wx:if="{{jiacai == 1}}">外卖（加菜）</view>
                </view>


                    <picker style=' border-top: 1rpx solid #efeff4;'  bindchange="bindPickerChange" range="{{wmtimearray}}" value="{{wmindex}}">
                        <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                                <image src="../../img/wmtime.png" style="margin-right: 5px;vertical-align: middle;width:35rpx; height:35rpx;"></image>
                            <view class="weui-cell__bd">立即送出</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" style="color:{{color}}">大约{{wmtimearray[wmindex]}}送达</view>
                        </view>
                    </picker>
                </view>
            </view>

            <!-- 到店自提 -->
            <view class="formhdzq" wx:if="{{index == 1}}">
                <view class="weui-cells weui-cells_after-title">
                    <view bindtap="ckwz" class="weui-cell weui-cell_access zqaddress" data-jwd="{{store.coordinates}}" hoverClass="weui-cell_active" url="">
                        <view class="weui-cell__bd">
                            <view class="zqdz1">自取地址</view>
                            <view class="zqdz2">{{store.address}}</view>
                            <view class="zqdz3">
                                <text>查看地图</text>
                            </view>
                        </view>
                        <view class="weui-cell__ft weui-cell__ft_in-access"></view>
                    </view> 


                 <picker style=' border-top: 1rpx solid #efeff4;' bindchange="bindChange" value="{{index}}" range="{{array}}"  wx:if="{{jiacai != 1 &&jiacai != 2}}">
                        <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                                <image src='../../img/ddlx.png' style="margin-right: 5px;vertical-align: middle;width:45rpx; height:45rpx;"></image>
                            <view class="weui-cell__bd" style='font-size:35rpx;'>订单类型</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" style="font-size:35rpx;color:#000;">{{array[index]}}</view>
                        </view>
                 </picker>

                 <!-- 加菜时显示 -->
                 <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active" wx:if="{{jiacai == 1 || jiacai == 2}}">
                                <image src='../../img/ddlx.png' style="margin-right: 5px;vertical-align: middle;width:45rpx; height:45rpx;"></image>
                            <view class="weui-cell__bd" style='font-size:35rpx;'>订单类型</view>
                             <view class="weui-cell__ft" style="font-size:35rpx;color:#000;" wx:if="{{jiacai == 2}}">自提（加菜）</view>
                 </view>

                    <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active" url="">
                        <view class="weui-cell__bd">自取时间</view>
                        <view class="weui-cell__ft weui-cell__ft_in-access picker" style="color:{{color}}">
                            <picker bindchange="bindDateChange" mode="date" start="{{datestart}}" style=" width: 200rpx" value='{{data}}'>
                                <view class="weui-input flex-row flex-y-center">{{date}}</view>
                            </picker>
                            <picker bindchange="bindTimeChange" end="23:59" mode="time" start="{{timestart}}" style=" width: 120rpx" value="{{time}}">
                                <view class="weui-input flex-row flex-y-center flex-x-center">{{time}}</view>
                            </picker>
                        </view>
                    </view>

                    <view class="weui-cell weui-cell_access" style="padding:8rpx 30rpx;">
                        <view class="weui-cell__bd">自取电话</view>
                        <view class="weui-cell__ft">
                            <input bindinput="KeyMobile" class="weui-input" placeholder="请输入自取联系电话" type="number" value="{{mobile}}"></input>
                        </view>
                    </view>

                    <view class="weui-cell weui-cell_access" style="padding:8rpx 30rpx;">
                        <view class="weui-cell__bd">自取联系人</view>
                        <view class="weui-cell__ft">
                            <input bindinput="KeyName" class="weui-input" placeholder="请输入自取联系人" value="{{name}}"></input>
                        </view>
                    </view>

                    <label bindtap="checkboxChange" class="weui-cell weui-check__label">
                        <view class="weui-cell__hd weui-check__hd_in-checkbox">
                            <icon class="weui-icon-checkbox_circle" size="21" type="circle" wx:if="{{!checked}}"></icon>
                            <icon class="weui-icon-checkbox_success" color="#34aaff" size="21" type="success" wx:if="{{checked}}"></icon>
                        </view>
                        <view catchtap="openxy" class="weui-cell__bd">同意
                           <text bindtap="look" style="color:{{color}}">《到店自取服务协议》</text>
                        </view>
                    </label>
                </view>
            </view>

<!-- --外卖自提-----订单的信息----------------------------------------------------------------------------------------- -->
            <view class="formbd">

                <view class="flex-row sjname" style=' border-top: 1rpx solid #efeff4;'>
                    <image src="{{url}}{{store.logo}}"></image>
                    <view class="flex-grow-1">{{store.name}}</view>
                </view>

                <view class="cart-item flex-row flex-y-center" wx:for="{{cart_list}}" wx:key="*this">
                    <view class="flex-grow-0 flex-y-center">
                        <image class="goods-pic" mode="aspectFill" src="{{item.img}}"></image>
                    </view>
                    
                    <view class="flex-grow-1 flex-col">
                        <view class="flex-grow-1">
                            <view wx:if="{{item.oneTwo===1}}">原</view>
                            <view wx:if="{{item.oneTwo===2}}">新</view>
                            <view class="goods-name">{{item.name}}</view>
                            <view class="attr-list">
                                <view class="attr-item">{{item.spec}}</view>
                            </view>
                        </view>

                        <view class="flex-grow-0 flex-row">
                            <view class="flex-grow-1 num">×{{item.num}}</view>
                            <view class="flex-grow-0 price">单价:￥{{item.price}}</view>
                            <view wx:if="{{}}" style='text-decoration:line-through' class="flex-grow-0 price">单价:￥{{item.price}}</view>
                        </view>
                    </view>
                </view>


                <view class="formbdyh flex-row flex-y-center" wx:if="{{pack_money!='' || pack_money!= 'undefined'}}">
                    <view class="flex-row flex-y-center flex-grow-1">
                         <image src='../../img/boxmoney.png' style="width:50rpx;height:50rpx;"></image>

                        <view>包装费</view>

                    </view>
                    <view class="flex-grow-0">￥{{pack_money}}</view>
                </view>



                <view style=' border-top: 1rpx solid #efeff4;' class="formbdyh flex-row flex-y-center" wx:if="{{index == 0}}">
                    <view class="flex-row flex-y-center flex-grow-1">
                         <image src='../../img/Delivery.png' style="width:65rpx;height:60rpx;"></image>
                        <view>配送费</view>
                    </view>

                    <view class="flex-grow-0" wx:if="{{index == 0}}">￥{{psf}}</view>
                    <view class="flex-grow-0" style="color:#999;" wx:if="{{index == 1}}">到店自取无配送费</view>
                </view>


                <!-- 获得积分 -->
                <view style='border-top: 1rpx solid #efeff4;' class="formbdyh flex-row flex-y-center" wx:if="{{getIntegral>0 && pay_type != '4'}}">
                    <view class="flex-row flex-y-center flex-grow-1">
                        <view class="yhhead bao">积</view>
                        <view>获得积分</view>
                    </view>
                    <view class="flex-grow-0">{{getIntegral}}分</view>
                
                </view>

                <!-- 在线支付满减 -->
                <view class="formftyh">
                <!-- 折扣 -->
                 <view class="formbdyh flex-row flex-y-center" wx:if='{{mjmoney!=0}}'>

                        <view class="flex-row flex-y-center flex-grow-1">
                             <image src='../../img/Discount.png' style="width:50rpx;height:30rpx;"></image>
                            <view>会员折扣</view>
                        </view>                        
                        <view class="flex-grow-0">-￥{{mjmoney}}</view>                                          
                        
                </view>
   
                   <!-- 在线支付满减 -->                  
                     <view class="formbdyh flex-row flex-y-center" wx:if="{{mj_reduce > 0 && pay_type != '4'}}">
                        <view class="flex-row flex-y-center flex-grow-1">
                           <image src='../../img/reduction.png' style="width:50rpx;height:30rpx;"></image>
                            <view>在线支付满减</view>
                        </view>                       

                         <view class="flex-grow-0">-￥{{mj_reduce}}</view>

                        <view class="flex-grow-0" style="color:#999;" wx:if="{{isnewuser=='1'}}">新用户不享有此优惠</view>                            </view>

                    <!-- 选择积分、支付部分积分 -->                  
                     <view class="formbdyh flex-row flex-y-center" wx:if='{{totalPrice>90&&used_total_score>=100}}'>
                        <radio color="{{color}}" style="transform:scale(0.8);" wx:for-items="{{items}}" checked="{{item.checked}}" data-index="index" bindtap="click"></radio>
                        <view class="flex-row flex-y-center flex-grow-1">
                            <view class="weui-cell__bd">使用{{used_total_score}}积分</view>
                        </view>
                        <view wx:if="{{!ischeck == true}}">请选择</view>
                        <view wx:elif="{{(ischeck==true)&&(totalPrice>90&&used_total_score>=100&&used_total_score<200)}}">-￥10</view>
                        <view wx:elif="{{(ischeck==true)&&(totalPrice>190&&used_total_score>=200&&used_total_score<300)}}">-￥20</view>
                        <view wx:else="{{(ischeck==true)&&(totalPrice>290&&used_total_score>=300)}}">-￥30</view>
                     </view>

                    <!-- 新客户下单立减 -->                   
                    <view class="formbdyh flex-row flex-y-center" wx:if="{{storeset.xyh_open=='1'&&isnewuser=='1'}}">
    
                        <view class="flex-row flex-y-center flex-grow-1">
                            <image src='../../img/new.png' style="width:50rpx;height:50rpx;"></image>
                            <view>新客户下单立减</view>
                        </view>
                        <view class="flex-grow-0">-￥{{storeset.xyh_money}}</view>
                    </view>

                   <view class="weui-cells weui-cells_after-title" style="border-top: 1rpx solid #f4f4f4;"  wx:if="{{yhq_active && pay_type != '4'}}">

                        <navigator bindtap="showcart" class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                            <view class="weui-cell__hd">
                                <image mode="aspectFit" src="../../img/personal/youhuiquan.png" style="margin-right: 15rpx;vertical-align: middle;width:40rpx; height: 40rpx;"></image>
                            </view>
                            <view class="weui-cell__bd">商家优惠券</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" wx:if="{{Coupons.length==0}}">暂无此商家优惠券</view>
<view class="weui-cell__ft_in-access" wx:if="{{Coupons.length>0&&yhqcoupon_reduce==0}}">
                            选择优惠券</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{Coupons.length>0&&yhqcoupon_reduce!=0}}">-￥{{yhqcoupon_reduce}}</view>

                        </navigator>
                    </view>

                   <view class="weui-cells weui-cells_after-title" style="border-top: 1rpx solid #f4f4f4;"  wx:if="{{hb_active && pay_type != '4'}}">

                        <navigator bindtap="hbshowcart" class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                            <view class="weui-cell__hd">
                                <image mode="aspectFit" src="../../img/hb.png" style="margin-right: 15rpx;vertical-align: middle;width:40rpx; height: 40rpx;"></image>
                            </view>
                            <view class="weui-cell__bd">红包</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access" wx:if="{{hbarr.length==0}}">暂无红包</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{hbarr.length>0&&hbcoupon_reduce==0}}">选择红包</view>
                            <view class="weui-cell__ft_in-access" wx:if="{{hbarr.length>0&&hbcoupon_reduce!=0}}">-￥{{hbcoupon_reduce}}</view>
                        </navigator>
                    </view>
                    <view class="sfk flex-row flex-y-center">
                        <view class="sfk-fv flex-grow-0">实付款</view>
                        <view class="flex-grow-1 xiaoji">
                            <view></view>
                            <view>小计</view>

                            <view wx:if="{{exchange_goods}}">￥{{totalPrice}}</view>
                            <view wx:if="{{!exchange_goods}}">{{integral_money}}积分</view>
                            <view wx:if="{{!exchange_goods}}" style='text-decoration:line-through;'>￥{{totalPrice}}</view>
                        </view>
                    </view>
                </view>

                <view class="formft">
                    <view class="weui-cells weui-cells_after-title">
                        <picker bindchange="bindcjPickerChange" range="{{cjarray}}" value="{{cjindex}}" wx:if="{{storeset.is_cj=='1'}}">
                            <view class="weui-cell weui-cell_access" hoverClass="weui-cell_active">
                                <view class="weui-cell__bd">{{storeset.cj_name}}</view>
                                <view class="weui-cell__ft weui-cell__ft_in-access">{{cjarray[cjindex]}}</view>
                            </view>
                        </picker>
                        <navigator class="weui-cell weui-cell_access" hoverClass="weui-cell_active" style="border-bottom: 1rpx solid #efeff4;" url="note">
                            <view class="weui-cell__bd" style="min-width:120rpx;">备注</view>
                            <view class="weui-cell__ft weui-cell__ft_in-access">{{note}}</view>
                        </navigator>
                    </view>
                </view>
            </view>
        </view>
    </view>


    <view class="footmj" wx:if="{{reduction.length>0&&mjindex!=null&&isnewuser=='2'}}">
      下单已满{{reduction[mjindex].full}}元，可减
      <text>{{reduction[mjindex].reduction}}</text>元
    </view>

    <view class="dbtjdd flex-row flex-y-center">
        <view class="dbtjddl flex-row flex-y-center">
            <view class="flex-grow-1">合计
              <text wx:if="{{exchange_goods}}">￥{{totalPrice}}</text>
              <text wx:if="{{!exchange_goods}}">{{integral_money}}积分</text>
              <text wx:if="{{!exchange_goods}}" style='text-decoration:line-through;'>￥{{totalPrice}}</text>
            </view>
            
        </view>
        <view class="dbtjddr">
            <form bindreset="formReset" bindsubmit="tjddformSubmit" reportSubmit="true">
                <button formType="submit" style="background:{{color}}">提交订单</button>
            </form>
        </view>
    </view>

</block>

<!-- 优惠券 -->
<view catchtap="closecart" class="share-modal {{share_modal_active?'active':''}}">
    <view class="share-modal-body">
        <view class="yhqhead">选择优惠券</view>
        <radio-group bindchange="radioChange" class="radio-group" name="radiogroup">
            <scroll-view scrollY style="height: 400rpx;">
                <label catchtap="xzq" data-full="{{item.full}}" data-kdje="{{item.reduce}}" data-rdid="{{item.id}}" data-type="代金券" wx:for="{{Coupons}}">
                    <view class="mdlist">
                        <view class="coupon-item unreceive" id="{{item.coupon_id}}">
                            <view class="item-left" style="background:#F65C5C">
                                <text class="num">{{item.reduce}}</text>
                                <text class="unit">元</text>
                            </view>
                            <view class="item-right">
                                <text class="name">{{item.name}}</text>
                                <text class="conditions" wx:if="{{item.full!='0.0'}}">满{{item.full}}可用</text>
                                <text class="conditions" wx:else>无门槛</text>
                                <text class="prefer">有效期至{{item.end_time}}</text>
                            </view>
                        </view>
                        <view class="radio">
                            <radio checked="{{activeradio==item.id}}" color="{{color}}" disabled="{{gwcinfo.money<item.full}}" value="{{item.id}}"></radio>
                        </view>
                    </view>
                </label>
                <view class="kong" wx:if="{{Coupons.length==0}}">
                    <image src="../../img/wushuju.png"></image>
                    <view>暂无此门店的优惠券</view>
                </view>
            </scroll-view>
        </radio-group>
    </view>
</view>

<!-- 红包 -->
<view catchtap="hbclosecart" class="share-modal {{hbshare_modal_active?'active':''}}">
    <view class="share-modal-body">
        <view class="yhqhead">选择红包</view>
        <radio-group bindchange="hbradioChange" class="radio-group" name="hbradiogroup">
            <scroll-view scrollY style="height: 400rpx;">
                <label catchtap="xzhb" data-full="{{item.full}}" data-kdje="{{item.reduce}}" data-rdid="{{item.id}}" data-type="红包" wx:for="{{hbarr}}">
                    <view class="mdlist">
                        <view class="coupon-item unreceive" id="{{item.coupon_id}}">
                            <view class="item-left" style="background:#F65C5C">
                                <text class="num">{{item.reduce}}</text>
                                <text class="unit">元</text>
                            </view>
                            <view class="item-right">
                                <text class="name">{{item.name}}</text>
                                <text class="conditions" wx:if="{{item.full!='0.0'}}">满{{item.full}}可用</text>
                                <text class="conditions" wx:else>无门槛</text>
                                <text class="prefer">有效期至{{item.end_time}}</text>
                            </view>
                        </view>
                        <view class="radio">
                            <radio checked="{{hbactiveradio==item.id}}" color="{{color}}" disabled="{{gwcinfo.money<item.full}}" value="{{item.id}}"></radio>
                        </view>
                    </view>
                </label>
                <view class="kong" wx:if="{{hbarr.length==0}}">
                    <image src="../../img/wushuju.png"></image>
                    <view>暂无可用红包</view>
                </view>
            </scroll-view>
        </radio-group>
    </view>
</view>
</view>

</view>
